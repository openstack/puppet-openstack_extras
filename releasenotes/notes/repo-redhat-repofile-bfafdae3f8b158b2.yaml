---
features:
  - |
    The new ``manifests::repo::redhat::redhat::repo_source_hash`` parameter has
    been added, so that repo file can be passed directly to configure yum
    repogitories on CentOS or RHEL nodes.
